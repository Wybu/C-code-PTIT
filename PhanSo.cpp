#include <stdio.h>
int UCLN(int a,int b){
    if (a%b!=0)
        return UCLN(b,a%b);
    else
        return b;
}
void rutgon(int a, int b){
	if 
}

int main (){
	int t;
	scanf ("%d", &t);
	int count=1;
	while (t--){	
		printf ("Case #%d:\n",count);
		int px, py, qx, qy;
		scanf ("%d%d%d%d", &px, &py, &qx, &qy);

		count++;
		printf("\n");
	}
}