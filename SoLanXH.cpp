#include <stdio.h>
#include <math.h>

void Nhap_Mang(int n, int a[101]) {
	for (int i = 0; i < n; i++) {
		scanf ("%d", &a[i]);
	}
}

int main(){
		int n, a[101];
		int count =0;
		Nhap_Mang(n, a);
		for (int i=0;i<n;i++){
			for (int j=0;j<n;j++){
				if[a]
			}
		}
}